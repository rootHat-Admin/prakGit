#include <stdio.h>

int main() {
  int i = 0;

  // Repeat while i is less than 5
  while (i < 5) {
    printf("i = %d\n", i); // Print current value of i
    i++;                   // Increment i
  }

  return 0;
}

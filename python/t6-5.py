# list of blocked IP addresses (list)
blocked_ips = ["192.168.0.10", "10.0.0.5", "127.0.0.1"]

# list of usernames (list) 
username = ["admin", "guest", "root"]

# list of open ports (list)
open_ports = [22, 80, 443, 3389]